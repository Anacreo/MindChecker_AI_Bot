services:
  - type: web
    name: mindchecker-ai-bot
    env: python
    plan: starter
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn bot:app --host 0.0.0.0 --port 10000
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: 8489671487:AAHRtLdxt5AhrjaM8HYcSMNkJNLg19AJL_k
